# Промпт для AI-агента: разработка Frontend-приложения «Budget Track Web»

## 1. Контекст и цель
Разработать современное веб-приложение для учёта личного бюджета. Пользователь должен иметь возможность добавлять доходы и расходы, категоризировать их и просматривать статистику.

## 2. Технологический стек
- **Фреймворк:** React (с TypeScript).
- **Сборка:** Vite.
- **Стилизация:** Tailwind CSS.
- **Маршрутизация:** React Router v6.
- **Тестирование:** Jest, React Testing Library.
- **Линтинг:** ESLint, Prettier.

## 3. Основные функции (минимум)
1. Добавление расходов и доходов с выбором категории.
2. Просмотр списка всех операций с фильтрацией по дате/категории.
3. Визуализация статистики (например, круговая диаграмма расходов по категориям).

## 4. Структура проекта
Сгенерируй следующую структуру:
```
/src
  /api
    api.ts
  /assets
  /components
    Header.tsx
    TransactionList.tsx
    CategorySelect.tsx
    AddTransactionForm.tsx
    Statistics.tsx
  /pages
    Dashboard.tsx
    AddTransaction.tsx
    StatisticsPage.tsx
  /utils
    types.ts
    helpers.ts
App.tsx
main.tsx
routes.tsx
```

## 5. Конфигурационные файлы
Создай и настрой:
- `package.json` (с зависимостями и скриптами).
- `vite.config.ts`.
- `tailwind.config.ts`.
- `.eslintrc.js` или `.prettierrc`.
- `.cursorrules` (если есть).

## 6. Реализация компонентов
Для каждого компонента:
- Используй TypeScript и Tailwind CSS.
- Реализуй обработку состояний: loading, error, success.
- Добавь комментарии к коду.
- Используй Mock-данные для демонстрации работы до подключения к API.

## 7. Роутинг
Настрой маршруты:
- `/` — Dashboard (главная страница).
- `/add` — добавление транзакции.
- `/stats` — статистика.

## 8. Тестирование
Создай минимум 3 автоматических теста:
1. Тест на добавление транзакции.
2. Тест на отображение списка транзакций.
3. Тест на обработку ошибки при загрузке данных.

## 9. Адаптивность
Обеспечь корректное отображение на десктопе и мобильных устройствах. Используй медиа-запросы Tailwind CSS.

## 10. Оптимизация и рефакторинг
Проведи рефакторинг кода, оптимизируй производительность, улучши читаемость. Добавь комментарии в сложных местах.

## 11. Документация процесса
В конце сгенерируй:
- README.md с инструкциями по запуску.
- development_report.md с описанием процесса, примерами промптов, проблемами и решениями.

## 12. Формат ответа
1. Сгенерируй структуру проекта и конфигурационные файлы (код).
2. Реализуй основные компоненты и страницы (код).
3. Добавь тесты (код).
4. Сгенерируй README и development_report.md (текст).
5. Дай инструкции по запуску проекта.

---

## 13. Управление категориями доходов и расходов

### Контекст
Категории расходов и доходов захардкожены в `src/utils/helpers.ts` в виде констант `expenseCategories` и `incomeCategories`. Категории используются в компонентах `CategorySelect`, `Statistics`, `Dashboard` и в API-слое. Цвета категорий также определены статически.

### Цель
Реализовать полный CRUD для категорий: пользователь может добавлять, удалять и редактировать собственные категории расходов и доходов. Категории должны сохраняться в localStorage и загружаться при старте приложения. Предустановленные (дефолтные) категории нельзя удалять, но можно редактировать их название.

### Функциональные требования

1. **Добавление категории**
   - Пользователь указывает: название (обязательное, уникальное в рамках типа), тип (расход/доход), цвет (выбор из предустановленной палитры).
   - Новая категория сразу появляется в селектах `CategorySelect` и в фильтрах на Dashboard.
   - Валидация: пустое название, дубликат названия, название длиннее 30 символов.

2. **Удаление категории**
   - Дефолтные категории нельзя удалить (кнопка неактивна или скрыта).
   - Пользовательские категории можно удалить.
   - Если существуют транзакции с удаляемой категорией — показать предупреждение и предложить перевести их на другую категорию того же типа или удалить вместе с категорией.
   - Удаление подтверждается через модальное окно.

3. **Редактирование категории**
   - Можно изменить название и цвет любой категории (включая дефолтные).
   - Изменение названия дефолтной категории влияет только на отображение, значение (value) остаётся прежним.
   - Для пользовательских категорий можно изменить и название, и цвет.

4. **Хранение**
   - Категории хранятся в localStorage (ключ `budget_track_categories`).
   - При первом запуске — инициализация дефолтными категориями.
   - Структура: `{ expense: CategoryInfo[], income: CategoryInfo[] }` с дополнительным полем `isDefault: boolean`.

5. **UI для управления**
   - Новая страница `/categories` с маршрутами в `routes.tsx`.
   - Список категорий с группировкой по типу (расходы/доходы).
   - Каждая категория — карточка с цветным индикатором, названием, кнопками «Редактировать» и «Удалить».
   - Кнопка «Добавить категорию» открывает модальное окно с формой.
   - Ссылка на страницу категорий в `Header.tsx`.

### Структура данных

Обновить `CategoryInfo` в `types.ts`:
```typescript
export interface CategoryInfo {
  value: string;          // уникальный идентификатор (slug)
  label: string;          // отображаемое название
  color: string;          // HEX-цвет
  isDefault: boolean;     // true для предустановленных
}
```

### Файлы, которые нужно изменить

| Файл | Изменение |
|------|-----------|
| `src/utils/types.ts` | Добавить `isDefault` в `CategoryInfo`, убрать фиксированные типы `ExpenseCategory`/`IncomeCategory` — заменить на `string` |
| `src/utils/helpers.ts` | Убрать `expenseCategories`/`incomeCategories` как константы, добавить функции `getDefaultCategories()`, `loadCategories()`, `saveCategories()` |
| `src/api/api.ts` | Добавить функции `addCategory()`, `deleteCategory()`, `updateCategory()`, обновить `fetchTransactions()` для работы с динамическими категориями |
| `src/components/Header.tsx` | Добавить пункт «Категории» в навигацию |
| `src/components/CategorySelect.tsx` | Принимать категории через пропсы вместо хардкода |
| `src/components/Statistics.tsx` | Принимать категории через пропсы |
| `src/pages/Dashboard.tsx` | Загружать категории из API, передавать в компоненты |
| `src/pages/StatisticsPage.tsx` | Загружать категории из API, передавать в компоненты |
| `src/routes.tsx` | Добавить маршрут `/categories` |

### Новые файлы

| Файл | Описание |
|------|----------|
| `src/pages/CategoriesPage.tsx` | Страница управления категориями |
| `src/components/CategoryForm.tsx` | Форма добавления/редактирования категории (модальное окно) |
| `src/components/CategoryCard.tsx` | Карточка категории с действиями |
| `src/components/ConfirmDialog.tsx` | Переиспользуемый диалог подтверждения |

### Тесты

Добавить минимум 3 теста:
1. Добавление новой категории — категория появляется в списке.
2. Удаление пользовательской категории — категория исчезает, транзакции переносятся.
3. Попытка удаления дефолтной категории — кнопка неактивна, удаление невозможно.

### Адаптивность

- На мобильных: карточки категорий в одну колонку, модальное окно на весь экран.
- На десктопе: карточки в две колонки, модальное окно по центру.

### Ограничения

- Минимум 1 категория каждого типа должна остаться (нельзя удалить все).
- Название категории — только буквы, цифры, пробелы и дефисы.
- Цвет — выбор из палитры из 12 предустановленных цветов.
